import 'package:flutter/material.dart';
import 'package:flutter_auth/pages/root_app.dart';

class Main_movie extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: RootApp(),
    );
  }
}
